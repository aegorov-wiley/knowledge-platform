# Local development profile: connects to the infrastructure in ../compose.yaml
# Run with: -Dspring.profiles.active=dev (or SPRING_PROFILES_ACTIVE=dev)
spring.datasource.url=jdbc:postgresql://localhost:5432/knowledge_platform
spring.datasource.username=knowledge_platform
spring.datasource.password=knowledge_platform

spring.kafka.bootstrap-servers=localhost:9092
